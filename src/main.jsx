import React from "react";
import ReactDOM from "react-dom/client";
import VolTerminal from "./components/VolTerminal.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VolTerminal />
  </React.StrictMode>
);
